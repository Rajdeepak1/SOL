#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n ;
        // for each 3 kg jar we need 2 kgs of berries 
        cout << 2*n << endl;
    }
    return 0;
}